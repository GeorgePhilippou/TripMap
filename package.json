{
  "name": "tripmap",
  "version": "1.34.1",
  "private": true,
  "description": "A collaborative, mobile-first trip planning map.",
  "scripts": {
    "test": "node scripts/validate.mjs && node --test test/*.test.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
